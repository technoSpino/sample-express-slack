{
  "name": "slack-example",
  "version": "1.0.0",
  "description": "A slack example app for filtinering posts and pushing a slack notification",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@slack/webhook": "^6.1.0",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
